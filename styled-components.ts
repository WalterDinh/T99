import * as styledComponents from 'styled-components/native/dist/native/index';

const {
    default: styled,
    css,
    ThemeProvider
} = styledComponents as any;

export {css, ThemeProvider};
export default styled;
